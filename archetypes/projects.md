+++
title = '{{ replace .File.ContentBaseName "-" " " | title }}'
date = {{ .Date }}
description = ''
draft = true

# Optional. Set for professional work, omit for personal projects.
# org = ''

# [[links]]
#   label = 'GitHub'
#   url = ''
# [[links]]
#   label = 'Site'
#   url = ''
+++

<!--
Anything below the front matter becomes the project's own page (a longer
write-up). Leave it empty and the listing renders as plain text with no page
behind it.

`date` is used only for ordering and is shown as a year. Delete the line
entirely for an undated entry - it will render without a date.
-->
